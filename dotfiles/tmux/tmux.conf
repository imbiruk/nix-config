set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'

# --- prefix ---
unbind C-b
set -g prefix C-a
bind C-a send-prefix
bind C-c new-session

# --- terminal ---
set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",xterm-ghostty:RGB"
set -g default-shell "/run/current-system/sw/bin/bash"
set -g default-command "/run/current-system/sw/bin/nu"

# --- scrollback ---
set -g history-limit 10000

setw -g mode-keys vi
bind -T copy-mode-vi v send -X begin-selection
bind -T copy-mode-vi y send -X copy-pipe-and-cancel "wl-copy"

# slower scroll: 3 lines per tick instead of 5
bind -T copy-mode-vi WheelUpPane   send -N3 -X scroll-up
bind -T copy-mode-vi WheelDownPane send -N3 -X scroll-down
bind -T copy-mode-vi C-u send -X halfpage-up \; send -X scroll-middle
bind -T copy-mode-vi C-d send -X halfpage-down \; send -X scroll-middle

# --- windows ---
bind c new-window -c "#{pane_current_path}"
bind X kill-window

# --- splits navigation ---
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# --- status ---
set -g status-position bottom
set -g status-interval 5

set -g status-style "bg=#21202e,fg=#6e6a86"
set -g status-left "#[fg=#908caa] #S "
set -g status-left-length 30
set -g status-right-length 30
set -g status-right "#[fg=#6e6a86]%I:%M %p "

set -g window-status-format " #I:#W "
set -g window-status-current-format "#[fg=#e0def4] #I:#W "

set -g pane-border-style "fg=#26233a"
set -g pane-active-border-style "fg=#403d52"

run-shell "bash $HOME/.config/tmux/plugins/tpm/tpm"
